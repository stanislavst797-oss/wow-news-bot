# Версии зафиксированы на minor: бот крутится 288 раз в сутки без присмотра,
# и сломанный свежий релиз наверху не должен его положить.
# Обновлять осознанно: pip install -U feedparser requests, затем поправить тут.
feedparser~=6.0.14
requests~=2.34
